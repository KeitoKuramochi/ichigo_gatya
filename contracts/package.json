{
  "name": "ichigo-gacha-nft-contracts",
  "version": "1.0.0",
  "private": true,
  "description": "ICHIGOガチャガチャ オンライン参加者向けNFT(ERC-1155 + EIP-712 claim voucher)コントラクト",
  "scripts": {
    "compile": "hardhat compile",
    "test": "hardhat test",
    "deploy:sepolia": "hardhat run scripts/deploy.js --network optimismSepolia",
    "deploy:mainnet": "hardhat run scripts/deploy.js --network optimism"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "@openzeppelin/contracts": "^5.0.2",
    "dotenv": "^16.4.5",
    "hardhat": "^2.22.5"
  }
}
